# Anthropic API key — used by Claude to extract travel segments from emails.
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional: override the SQLite location or point at Postgres (Railway sets this).
# DATABASE_URL=sqlite:///./travel.db
